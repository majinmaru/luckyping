## 목표

기능은 그대로 두고, 프로젝트가 **GitHub Pages + 본인 Supabase(`ugdsgueyidscjfluymhg`) + AWS Lambda** 만으로 돌아가도록 모든 Lovable 흔적과 잘못된 Supabase URL(`wtpplbyvhhmuqklyynce`, `dlybhuneuwukkvyfrmmh`)을 제거/교체합니다.

## 변경 파일

### 1. Supabase URL 통일 (`ugdsgueyidscjfluymhg`)
- **`.env`** — 로컬 개발용. URL을 새 프로젝트로 교체. `VITE_SUPABASE_PUBLISHABLE_KEY` 는 placeholder(`REPLACE_ME`)로 두고 사용자가 본인 anon key를 채워 넣도록 안내. `VITE_SUPABASE_PROJECT_ID` 줄은 삭제(앱에서 안 씀).
- **`.env.example`** — 주석/URL 모두 `ugdsgueyidscjfluymhg`로.
- **`MIGRATION.md`** — `wtpplbyvhhmuqklyynce`, `dlybhuneuwukkvyfrmmh` 전부 `ugdsgueyidscjfluymhg`로. "Lovable Cloud" 문구 제거하고 "구 Supabase 프로젝트"로 표현.
- **`lambda/README.md`** / **`lambda/template.yaml`** — 예시 URL 교체.
- **`supabase/config.toml`** — `project_id` 를 `ugdsgueyidscjfluymhg` 로 교체 (Supabase CLI용 메타데이터).

### 2. Lovable 코드/메타 제거
- **`index.html`**
  - `<meta name="author" content="Lovable" />` 삭제
  - `og:image`, `twitter:image` — Lovable R2 호스팅 이미지 URL 제거(태그 자체 삭제, 추후 사용자가 자체 이미지 추가 가능)
  - `twitter:site` `@Lovable` 삭제
- **`package.json` / `package-lock.json`** — `@lovable.dev/cloud-auth-js` 의존성 잔여물 제거(`bun remove` 로 lock 동기화). 코드에서는 이미 미사용.
- **`playwright.config.ts` / `playwright-fixture.ts`** — `lovable-agent-playwright-config` 의존. 이 패키지는 Lovable 샌드박스 전용이므로 GitHub Actions 환경에서 무의미. 표준 `@playwright/test` 기반으로 단순화 (또는 e2e를 안 쓰면 두 파일 삭제). **본 계획에서는 두 파일 삭제 + `package.json` 에서 `lovable-agent-playwright-config` 제거** (현재 CI에서 playwright 안 돌리고 있고, vitest 만 사용 중).
- **`.lovable/plan.md`** — Lovable 에디터 작업 메모. 삭제.

### 3. GitHub Actions 점검 (`.github/workflows/deploy.yml`)
이미 GitHub Secrets(`VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`, `VITE_LAMBDA_API_BASE`, `VITE_BASE_PATH`) 기반으로 빌드함. 변경 없음. README/MIGRATION에 새 URL로 Secret 갱신 안내만 추가.

### 4. Auto-generated 파일 정책
- `src/integrations/supabase/client.ts` 와 `src/integrations/supabase/types.ts` 는 Lovable이 만든 것이지만, **GitHub만으로 동작**하므로 그대로 commit해 사용합니다. 코드 내용에는 Lovable URL/키가 하드코딩돼 있지 않고 env만 읽으므로 안전합니다. 상단 주석에서 "auto-generated by Lovable" 같은 표현이 있다면 일반 주석으로 정리합니다.

## 사용자가 직접 해야 할 것 (코드 외)

1. 새 Supabase 프로젝트 `ugdsgueyidscjfluymhg` 의 **anon key**를 받아
   - 로컬 `.env` 의 `VITE_SUPABASE_PUBLISHABLE_KEY` 채우기
   - GitHub repo → Settings → Secrets → `VITE_SUPABASE_PUBLISHABLE_KEY` 갱신
   - `VITE_SUPABASE_URL` Secret 도 `https://ugdsgueyidscjfluymhg.supabase.co` 로 갱신
2. 새 Supabase 의 Authentication → URL Configuration 에서 Site URL / Redirect URLs 에 GitHub Pages URL(`https://majinmaru.github.io/luckyping/` 등) 등록.
3. Google/Apple OAuth Provider 의 Client ID/Secret 입력 (Lovable에 등록돼 있던 것은 새 프로젝트로 옮겨야 함).

## 검증

- `grep -ri "lovable\|wtpplbyvhhmuqklyynce\|dlybhuneuwukkvyfrmmh"` 결과가 빈 결과여야 함 (build artifact/.workspace 제외).
- `bun run build` 정상 동작.
- 코드상 Supabase 호출은 100% env 기반.

## 기능 영향

없음. UI/로직/DB 스키마는 그대로. Lovable 프리뷰만 동작하지 않게 되며 GitHub Pages 배포에는 영향 없음.
